int max(int a = 0, int b = 2) {
    if(a > b)
        return (a);
    return (b);

    // the return value must always be inside parantheses.
}

int min(int a = 0, int b = 2) {
    if(a < b)
        return (a);
    return (b);

    // the return value must always be inside parantheses.
}